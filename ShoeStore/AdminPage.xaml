<Page x:Class="ShoeStore.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ShoeStore"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="Страница администратора">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="MainStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Конвертеры -->
            <local:AdminImagePathConverter x:Key="AdminImagePathConverter"/>
            <local:AdminDiscountToVisibilityConverter x:Key="AdminDiscountToVisibilityConverter"/>
            <local:AdminDiscountColorConverter x:Key="AdminDiscountColorConverter"/>
            <local:AdminRowBackgroundConverter x:Key="AdminRowBackgroundConverter"/>
            <local:AdminPriceDisplayConverter x:Key="AdminPriceDisplayConverter"/>

            <!-- Стиль для кнопки выхода -->
            <Style x:Key="LogoutButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="#E53935"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Padding" Value="10 5"/>
                <Setter Property="Margin" Value="10 0 0 0"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Style>

            <!-- Стиль для фильтров -->
            <Style x:Key="FilterLabelStyle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="#333"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,0,5,0"/>
            </Style>

            <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
                <Setter Property="Width" Value="150"/>
                <Setter Property="Margin" Value="0,0,10,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>

            <Style x:Key="FilterTextBoxStyle" TargetType="TextBox">
                <Setter Property="Width" Value="200"/>
                <Setter Property="Margin" Value="0,0,10,0"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Padding" Value="5"/>
            </Style>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок с информацией о пользователе -->
        <Border Grid.Row="0" Padding="10" BorderBrush="#ddd" BorderThickness="0 0 0 1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Заголовок слева -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource HeaderText}" Text="УПРАВЛЕНИЕ ТОВАРАМИ - АДМИНИСТРАТОР"/>
                </StackPanel>

                <!-- Информация о пользователе справа -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <StackPanel Orientation="Vertical" Margin="0 0 10 0">
                        <TextBlock Text="Вы вошли как:" 
                                   FontSize="10" 
                                   Foreground="#666"
                                   HorizontalAlignment="Right"/>
                        <TextBlock x:Name="UserInfoTextBlock" 
                                   FontSize="14" 
                                   FontWeight="Bold"
                                   Foreground="#333"
                                   HorizontalAlignment="Right"/>
                    </StackPanel>
                    <Button x:Name="LogoutButton" 
                            Content="ВЫЙТИ" 
                            Click="LogoutButton_Click" 
                            Width="80"
                            FontSize="12"
                            Style="{StaticResource LogoutButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Панель фильтрации и поиска -->
        <Border Grid.Row="1" Padding="10" BorderBrush="#ddd" BorderThickness="0 0 0 1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Строка 1: Поиск -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Поиск:" 
                               Style="{StaticResource FilterLabelStyle}"/>
                    <TextBox x:Name="txtSearch" 
                             Style="{StaticResource FilterTextBoxStyle}"
                             TextChanged="TxtSearch_TextChanged"/>

                    <Button x:Name="btnRefresh" 
                            Content="Обновить данные" 
                            Click="BtnRefresh_Click"
                            Padding="8 4"
                            FontSize="12"
                            Margin="10,0,0,0"/>
                </StackPanel>

                <!-- Строка 2: Фильтры -->
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                        <TextBlock Text="Тип:" Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox x:Name="cmbTypeFilter" 
                                  Style="{StaticResource FilterComboBoxStyle}"
                                  SelectionChanged="CmbTypeFilter_SelectionChanged">
                            <ComboBoxItem Content="Все типы" Tag="All"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                        <TextBlock Text="Категория:" Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox x:Name="cmbCategoryFilter" 
                                  Style="{StaticResource FilterComboBoxStyle}"
                                  SelectionChanged="CmbCategoryFilter_SelectionChanged">
                            <ComboBoxItem Content="Все категории" Tag="All"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                        <TextBlock Text="Производитель:" Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox x:Name="cmbManufacturerFilter" 
                                  Style="{StaticResource FilterComboBoxStyle}"
                                  SelectionChanged="CmbManufacturerFilter_SelectionChanged">
                            <ComboBoxItem Content="Все производители" Tag="All"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Поставщик:" Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox x:Name="cmbSupplierFilter" 
                                  Style="{StaticResource FilterComboBoxStyle}"
                                  SelectionChanged="CmbSupplierFilter_SelectionChanged">
                            <ComboBoxItem Content="Все поставщики" Tag="All"/>
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Основной контент с вертикальным скроллом -->
        <Border Grid.Row="2" Background="White" Margin="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="5">
                <ListBox Name="ListBoxTovar" 
                         ItemsSource="{Binding}"
                         SelectionMode="Single"
                         BorderThickness="0"
                         Background="Transparent">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- Каждая карточка товара как строка таблицы -->
                            <Border BorderBrush="#CCCCCC"
                                    BorderThickness="1"
                                    Padding="15"
                                    Margin="10,5,10,5"
                                    Cursor="Hand"
                                    MouseUp="Item_MouseUp">
                                <!-- Динамический фон строки -->
                                <Border.Background>
                                    <MultiBinding Converter="{StaticResource AdminRowBackgroundConverter}">
                                        <Binding Path="Discount"/>
                                        <Binding Path="Quantity"/>
                                    </MultiBinding>
                                </Border.Background>

                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Редактировать" Click="EditProductMenuItem_Click"/>
                                        <MenuItem Header="Удалить" Click="DeleteProductMenuItem_Click"/>
                                    </ContextMenu>
                                </Border.ContextMenu>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Фото товара -->
                                    <Border Grid.Row="0" Grid.Column="0" 
                                            Grid.RowSpan="8" 
                                            Margin="0,0,15,0"
                                            Width="100" 
                                            Height="100">
                                        <Image Source="{Binding Photo, Converter={StaticResource AdminImagePathConverter}}" 
                                               Stretch="Uniform"/>
                                    </Border>

                                    <!-- Первая строка: Категория товара и Наименование товара -->
                                    <Grid Grid.Row="0" Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="Категория товара:" 
                                                   FontWeight="SemiBold"
                                                   Foreground="#666666"
                                                   VerticalAlignment="Center"
                                                   FontSize="12"
                                                   Width="130"/>
                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Categories.Category}" 
                                                   VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   FontSize="12"
                                                   Margin="5,0,0,0"/>
                                    </Grid>

                                    <!-- Наименование товара -->
                                    <TextBlock Grid.Row="0" Grid.Column="1" 
                                               Text="{Binding Types.Type}" 
                                               FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="#333333"
                                               TextWrapping="Wrap"
                                               HorizontalAlignment="Right"
                                               Margin="0,0,0,5"/>

                                    <!-- Вторая строка: Описание товара и Скидка -->
                                    <Grid Grid.Row="1" Grid.Column="1" Margin="0,5,0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="Описание товара:" 
                                                   FontWeight="SemiBold"
                                                   Foreground="#666666"
                                                   VerticalAlignment="Center"
                                                   FontSize="12"
                                                   Width="130"/>
                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Description}" 
                                                   VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   FontSize="12"
                                                   Margin="5,0,0,0"
                                                   MaxHeight="40"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Grid>

                                    <!-- Скидка -->
                                    <Border Grid.Row="1" Grid.Column="1" 
                                            CornerRadius="3"
                                            Padding="5 2"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top"
                                            Margin="0,2,0,0">

                                        <Border.Background>
                                            <Binding Path="Discount" Converter="{StaticResource AdminDiscountColorConverter}"/>
                                        </Border.Background>

                                        <Border.Visibility>
                                            <Binding Path="Discount" Converter="{StaticResource AdminDiscountToVisibilityConverter}"/>
                                        </Border.Visibility>

                                        <TextBlock Text="{Binding Discount, StringFormat={}{0}%}" 
                                                   Foreground="White"
                                                   FontWeight="Bold"
                                                   FontSize="11"/>
                                    </Border>

                                    <!-- Третья строка: Производитель -->
                                    <Grid Grid.Row="2" Grid.Column="1" Margin="0,0,0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="Производитель:" 
                                                   FontWeight="SemiBold"
                                                   Foreground="#666666"
                                                   VerticalAlignment="Center"
                                                   FontSize="12"
                                                   Width="130"/>
                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Manufactureres.Manufacturer}" 
                                                   VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   FontSize="12"
                                                   Margin="5,0,0,0"/>
                                    </Grid>

                                    <!-- Четвертая строка: Поставщик -->
                                    <Grid Grid.Row="3" Grid.Column="1" Margin="0,0,0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="Поставщик:" 
                                                   FontWeight="SemiBold"
                                                   Foreground="#666666"
                                                   VerticalAlignment="Center"
                                                   FontSize="12"
                                                   Width="130"/>
                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Supplieres.Supplier}" 
                                                   VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   FontSize="12"
                                                   Margin="5,0,0,0"/>
                                    </Grid>

                                    <!-- Пятая строка: Цена -->
                                    <Grid Grid.Row="4" Grid.Column="1" Margin="0,0,0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="Цена:" 
                                                   FontWeight="SemiBold"
                                                   Foreground="#666666"
                                                   VerticalAlignment="Center"
                                                   FontSize="12"
                                                   Width="130"/>

                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <!-- Цена без скидки -->
                                            <TextBlock Text="{Binding Price, StringFormat={}{0} ₽}" 
                                                       FontSize="14"
                                                       FontWeight="Bold"
                                                       Margin="5,0,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#E53935"/>
                                                        <Setter Property="TextDecorations" Value="None"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Discount, Converter={StaticResource AdminDiscountToVisibilityConverter}}" Value="Visible">
                                                                <Setter Property="Foreground" Value="Red"/>
                                                                <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>

                                            <!-- Итоговая цена -->
                                            <TextBlock FontSize="16"
                                                       FontWeight="Bold"
                                                       Foreground="Black"
                                                       Margin="5,0,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Discount, Converter={StaticResource AdminDiscountToVisibilityConverter}}" Value="Visible">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{StaticResource AdminPriceDisplayConverter}">
                                                        <Binding Path="Price"/>
                                                        <Binding Path="Discount"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Шестая строка: Единица измерения -->
                                    <Grid Grid.Row="5" Grid.Column="1" Margin="0,0,0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="Единица измерения:" 
                                                   FontWeight="SemiBold"
                                                   Foreground="#666666"
                                                   VerticalAlignment="Center"
                                                   FontSize="12"
                                                   Width="130"/>
                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Measurement}" 
                                                   VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   FontSize="12"
                                                   Margin="5,0,0,0"/>
                                    </Grid>

                                    <!-- Седьмая строка: Количество на складе -->
                                    <Grid Grid.Row="6" Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="Количество на складе:" 
                                                   FontWeight="SemiBold"
                                                   Foreground="#666666"
                                                   VerticalAlignment="Center"
                                                   FontSize="12"
                                                   Width="130"/>
                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Quantity}" 
                                                   VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   FontSize="12"
                                                   Margin="5,0,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#333333"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Quantity}" Value="0">
                                                            <Setter Property="Foreground" Value="Red"/>
                                                            <Setter Property="FontWeight" Value="Bold"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>

                                    <!-- Артикул -->
                                    <TextBlock Grid.Row="7" Grid.Column="1" 
                                               Text="{Binding Article, StringFormat={}Артикул: {0}}" 
                                               FontSize="10"
                                               Foreground="#999999"
                                               HorizontalAlignment="Right"
                                               Margin="0,5,0,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </ScrollViewer>
        </Border>

        <!-- Кнопка возврата -->
        <Border Grid.Row="3" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BackButton" 
                        Content="ВЕРНУТЬСЯ НА ГЛАВНУЮ" 
                        Click="BackButton_Click" 
                        Width="250" 
                        FontSize="16" 
                        HorizontalAlignment="Left"/>

                <Button x:Name="AddProductButton" 
                        Content="ДОБАВИТЬ ТОВАР" 
                        Click="AddProductButton_Click" 
                        Width="200" 
                        FontSize="14" 
                        Margin="20,0,0,0"
                        Background="#2196F3"
                        Foreground="White"
                        FontWeight="Bold"/>

                <Button x:Name="ViewOrdersButton" 
                        Content="ЗАКАЗЫ" 
                        Click="ViewOrdersButton_Click" 
                        Width="200" 
                        FontSize="14" 
                        Margin="20,0,0,0"
                        Background="#4CAF50"
                        Foreground="White"
                        FontWeight="Bold"/>

                <TextBlock x:Name="txtStatistics" 
                           Margin="20,0,0,0"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="#666"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>